<div class="bg-white rounded-xl shadow-sm p-6">
  <div class="flex items-center justify-between mb-6">
    <div class="h-6 w-48 bg-gray-200 animate-pulse rounded"></div>
  </div>
  <div class="space-y-6">
    {
      Array(3)
        .fill(null)
        .map((_, i) => (
          <div class="space-y-2">
            <div class="flex items-center justify-between">
              <div class="h-5 w-40 bg-gray-200 animate-pulse rounded" />
              <div class="h-4 w-24 bg-gray-200 animate-pulse rounded" />
            </div>
            <div class="w-full bg-gray-200 rounded-full h-2">
              <div
                class="bg-gray-300 animate-pulse h-2 rounded-full"
                style={{ width: `${Math.random() * 100}%` }}
              />
            </div>
          </div>
        ))
    }
  </div>
</div>
